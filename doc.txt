alert_priority:

1. await HandleAlertUpdateAppAsync(remoteConfig) // alert_priority_1 (doc)

alert not priority:
+ Alert.alert('Wohoo!', texts.purchase_success) // alert whenever purchase success or FORCE SET PREMIUM AFTER SPLASH-SCREEN